"""hint — build HTML as a tree of description values and render it once.

hint is not templating. HTML is built in code as a tree of plain description
values (``Element``, ``RawHtml``, ``str``) by a constructor per tag, and a single
``render`` step converts the tree to a string at the edge. See the README for the
full API.

This module is a placeholder; the core lands in the ``feat/extraction-core`` sub-PR.
"""
